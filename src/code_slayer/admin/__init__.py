"""Operator administration: service, Ollama discovery, Tailscale, updates.

None of these modules are a security authority. The browser may request
only the fixed operations they expose. Subprocess argv is always a
code-owned tuple — never a shell string assembled from HTTP input.
"""
