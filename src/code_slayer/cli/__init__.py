"""Minimal CLI wiring for Phase 1.

Only what's needed to prove the entry point works and to exercise the
identity foundation from the command line — the full CLI surface (task,
status, resume, audit, checkpoint, workers) belongs to later phases.
"""
